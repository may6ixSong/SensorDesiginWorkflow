# ARBOR Web - local environment
# Copy to .env and adjust if the API runs on a different host/port.

# BE Dev API Base URL (킥오프 프롬프트 [A]: default 포트 사용)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# TODO: SSO 연동 지점 - 실제 IdP 연동 전까지 AppShell의 "사용자 스위처"로 로그인 목업.
# 별도 FE 환경변수는 필요 없음 (BE의 /auth/switchable-users, /auth/dev-login 사용).
